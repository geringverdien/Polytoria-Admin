# Polytoria admin script

not a lot of features yet,

has command suggestion, partial player name matching and decent ui (all hail Instance2Lua!)

## Building
1. `npm install -g polybundle` (requires node.js)
2. `polybundle` in root directory
